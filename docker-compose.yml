services:
  db:
    image: postgres:16-alpine
    container_name: certi-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-certi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-certi}
      POSTGRES_DB: ${POSTGRES_DB:-certi}
    ports:
      - "5432:5432"
    volumes:
      - certi_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-certi}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: certi-redis
    ports:
      - "6379:6379"
    volumes:
      - certi_redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./apps/api
    container_name: certi-api
    env_file:
      - .env
    environment:
      POSTGRES_HOST: db
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./apps/api:/app

volumes:
  certi_pgdata:
  certi_redisdata:
